import Layout from '../layout/DefaultLayout.tsx';
import TanStackTable from './tanstack-table/TanStackTable.tsx';

const Settings = () => {
  return (
    <Layout model='Ajustes'>
      <TanStackTable />
    </Layout>
  );
};

export default Settings;
